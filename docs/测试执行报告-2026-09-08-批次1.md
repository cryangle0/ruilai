# 测试执行报告（批次 1：问题 #1–10）

执行日期：2026-09-08  
范围：管理端工作台、代理、二级审核、待分配、采购页签与侧栏待办。

## 结果

| 问题 | 结果 | 验证证据 |
|---|---|---|
| #1 | 通过 | 无“查看全部待办”；待办区 6 列自适应铺满；UI 契约通过 |
| #2 | 通过 | 全国城市数据统一展示；父级未授权城市禁选，未放宽服务端销售范围约束 |
| #3 | 通过 | 一级、二级状态列统一为 150px |
| #4 | 通过 | 二级编辑改为与一级一致的 1040px 分区三列表单 |
| #5 | 通过 | 审核详情移除合作协议；L2 列表/详情均填充登录账号；后端编译通过 |
| #6 | 通过 | 审核详情 900px 横向，弹窗高度按内容自适应 |
| #7 | 通过 | 审核及采购页签显示数字（含 0）；采购补已驳回页签 |
| #8 | 通过 | 侧栏仅非零显示；审核、重新分配、采购处理后主动刷新角标 |
| #9 | 通过 | 待分配详情改为 820px 横向内容弹窗 |
| #10 | 通过 | 重新绑定改为 920px 横向内容弹窗 |

## 自动验证

- `node --test --test-name-pattern='^(issue 1 removes|issues 2 through 5|issues 6 through 10)' tests/issue-ui-contract.test.mjs`：3/3 通过。
- `npm run build`（Web）：通过。
- `mvn -s /tmp/ruilai-maven-settings.xml -DskipTests package`：后端编译/打包验证。
- 后端全量测试基线：30 个测试中 8 个既有交易域测试失败，集中于采购/销售/返货；与本批 AgentService 修改无关，留到对应业务批次修复。

## 未执行

- 本批未发布线上，最终全部批次通过后统一发布。
- 未执行审核通过/驳回或代理改绑的生产数据写入操作；避免改动演示数据。
