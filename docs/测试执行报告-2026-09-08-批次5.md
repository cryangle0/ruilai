# 测试执行报告（批次 5：问题 #41–50）

执行日期：2026-09-08  
范围：管理端异常、销售客户、数据统计和角色权限。

## 结果

| 问题 | 结果 | 验证证据 |
|---|---|---|
| #41 | 通过 | 同手机号且姓名/地址一致时识别为同一客户，多商品激活不再误报重复 |
| #42 | 通过 | 两个激活页签均查询 `activate` 异常，并按 SN 是否归属二级区分直售/分销 |
| #43 | 通过 | 日期控件前增加“报警时间” |
| #44 | 通过 | 异常弹窗采用内容高度，不再保留固定空白 |
| #45 | 通过 | 客户列表只保留所选区间内存在销售/激活 SN 的客户 |
| #46 | 通过 | 重复手机号、重复地址可独立筛选；同一客户可并列显示两个标签 |
| #47 | 通过 | KPI 分两行铺满，区间激活数位于第二行首位 |
| #48 | 通过 | 二级范围直售数固定为 0，界面隐藏直售 KPI |
| #49 | 通过 | 标题统一为销售渠道；补齐一级代理排行，图表按 2/3+1/3、1/2+1/2 布局 |
| #50 | 通过 | 权限选项展示中文功能说明，不再暴露内部权限编码 |

## 自动验证

- Web UI 契约：15/15 通过。
- Web `npm run build`：通过。
- 后端定向测试：17/17 通过。
- 后端全量测试：42/42 通过。

## 业务约束

- #41 只有同手机号对应的姓名或地址发生冲突时，才认定为重复客户；同一客户购买多件商品不构成异常。
- #42 激活异常类型统一为归属地异常、客户信息重复、IP 异常；渠道只由 SN 是否归属二级决定。
- #48 “直售”专指一级直接销售给 C 端，二级面向 C 端的数据保留在销售/激活统计中。
- 本批未发布线上，最终全量回归后统一发布。
