# 测试执行报告（批次 3：问题 #21–30）

执行日期：2026-09-08  
范围：管理端采购、销售、库存、返货、异常、客户列表及采购审核。

## 结果

| 问题 | 结果 | 验证证据 |
|---|---|---|
| #21 | 通过 | 六个页面统一使用 `SearchPanel` 和 `KpiCards` 设计组件 |
| #22 | 通过 | 采购、返货、异常、客户直接进入默认本月；显式历史跳转仍不过滤 |
| #23 | 通过 | 采购列表、详情和审核中的“配件”文案统一改为“单品” |
| #24 | 通过 | 非标行按已维护商品、弹力带、腰带范围选择，并随会签请求持久化 |
| #25 | 通过 | 前后端统一只接受完整 SN 号段；`1-10` 不再前端误判为 10 条 |
| #26 | 通过 | 采购页签含待处理、会签中、已完成、已驳回及准确数量 |
| #27 | 通过 | 驳回原因写入数据库并在采购详情显示 |
| #28 | 通过 | 销售页签移除数量角标 |
| #29 | 通过 | 销售时间筛选前增加“下单时间”标签 |
| #30 | 通过 | 销售详情改横向内容高度弹窗 |

## 自动验证

- Web UI 契约：批次 3 共 3 组，3/3 通过。
- Web `npm run build`：通过。
- 后端 `PurchaseServiceTest` + `SnRangesTest`：5/5 通过。
- 后端本批代码编译通过。

## 业务约束

- #24 非标规格来自商品库已维护范围，不允许任意文本录入。
- #25 不接受纯数字作为 SN，避免创建无效 SN；示例合法格式为 `RL202608010001-RL202608010010`。
- 本批未发布线上，最终全量回归后统一发布。
