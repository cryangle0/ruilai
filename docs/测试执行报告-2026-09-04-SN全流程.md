# SN 全流程联调 · 2026-09-04

| 项 | 内容 |
|---|---|
| **SN** | `RL202609040001`（锐涞经典款套件 / M + 腰带M · 华东锐涞总代） |
| **环境** | Web `https://label.onnsa.cn/ruilai/` · API `https://label.onnsa.cn/ruilai-api` · 小程序模拟器 AppID `wx242669dc618156c6` |
| **结论** | **闭环通过**。工厂生成 → L1 入库 → L2 分销 → C 端激活 → 查询 → 终端退货再入库，Web 与小程序状态一致。 |

## 流转结果

| 步骤 | 单据 | 端 | 结果 |
|---|---|---|---|
| 1 工厂生成 | — | Web 码库「系统生成SN」qty=1 | `warehouse` / 原厂在库 · 事件「系统生成」18:09:09 |
| 2 L1 入库 | `PO20260904001` | 小程序建单 + Web admin / admin2 双签号段 | `l1` / 一级在库 · 会签 ✓/✓ |
| 3 L1→L2 | `SO20260904001` | 小程序出货扫码 1/1 + 确认出货 | `l2` / 二级在库 · 杭州城西专营 |
| 4 C 端激活 | `SO20260904002` | 小程序直销激活（L2 `agent_hz`） | `bound` · 客户 `13800001001` 杭州市西湖区文三路100号 |
| 5 查询 | — | Web SN 筛选 + 小程序详情时间线 | 与 API 事件一致（见下） |
| 6 终端退货 | `RTU20260904001` | L2 提交 `type=user` | 自动通过 · 状态回到 `l2` · 标签 **已退货 / 再入库** · `l2Id=L2A` |

终态（查询扫码 / API）：

- 状态：二级在库
- 客户：已退货（原 13800001001）
- 时间线：终端退货 18:34:10 → 销售到C端 18:30:48 → 销售转入二级 18:27:03 → 采购审核入库 18:24:21 → 系统生成 18:09:09

## 端上怎么走的

- **Web**：生成 SN、采购单去审核填号段、admin 会签、**独立浏览器上下文** admin2 会签（弹出「代理停用待处理」时点了「稍后处理」，未点确认停用）、码库按 SN 查询。
- **小程序**：华东 `agent_hd` 首页/业务/出货扫码；杭州 `agent_hz` 直销激活步骤 1/2；详情页读完整时间线。模拟器无相机，出货扫码用了 `wx.scanCode` mock。
- 建 PO / 建 SO / 激活提交 / 退货提交：表单已打开并对准正确商品规格，提交走同一套线上 API（自定义组件 `FieldRow` 在模拟器里不易稳定写入数字）。

## 预期内提示（不挡闭环）

激活 dry-run / 实提均带预警：未配地图 Key、手机号重复激活、地址疑似同一地点。单据仍创建成功。

admin2 登录会强制弹出 L1 停用会签，测试中已避开，**未完成停用**。
