# 测试执行报告（批次 9：问题 #81 及全量回归）

执行日期：2026-09-09

## #81 验证

分销激活异常查询已由错误的 `scan` 维度修正为 `activate`，再按 SN 的 `l2Id` 归属区分分销与直售。管理端和小程序均读取 `exception_ticket`，小程序不再因维度错误而显示空列表。

## 81 条状态

- `issues.json` 共 81 条，待验证项 0 条。
- 实施矩阵已覆盖 #1–81。
- 各批次报告已记录对应问题、代码映射和验证依据。

## 全量回归

- Web UI 契约：16/16 通过。
- Web TypeScript 检查及生产构建：通过。
- 小程序 TypeScript 检查：通过。
- 小程序页面测试：16/16 通过。
- 小程序采购模型测试：5/5 通过。
- 微信小程序生产构建：通过。
- 后端 Maven 全量测试：44/44 通过。
- `issues.json` 解析及 Git diff 检查：通过。

## 发布结果

- 发布前备份：`label:/home/ubuntu/backups/ruilai-20260908-161618`（后端 JAR、环境文件、Web、数据库）。
- Web：已发布至 `https://label.onnsa.cn/ruilai/`，HTTP 200。
- 后端：已发布并保持 `ruilai.service` active，内网及公网健康检查通过。
- 数据库：Flyway V6、V7 成功执行，当前版本 v7。
- 小程序：已于 2026-09-09 01:05 覆盖上传原开发版本 `0.2.9`，AppID `wx242669dc618156c6`，微信开发者工具 CLI 返回 `✔ upload`。

## 最终复核补正

- #7：线上核对二级审核四个页签均显示数字（含 0）；补充右上角上标样式。待分配页按原型只有待分配池，不虚构审核状态页签。
- #37：原实现只覆盖新发生的返货。现已对历史退货单补充只读流转事件及处理说明，包括反馈中的 `RL202608010041`。
- #71：退货单未直接保存客户快照时，从关联 SN 的当前/历史客户快照补齐详情。
- 模拟器自动化未作为通过依据：复核期间自动化窗口状态异常，已停止且未再新开窗口。

## 最终发布结果

- 管理端与后端已于 2026-09-09 01:06 部署至 `https://label.onnsa.cn/ruilai/`，Web 与 API 探活均为 HTTP 200。
- 线上 #7 实测：
  - 二级审核：待审核 `0`、已通过 `3`、已驳回 `0`、全部 `3`，均显示右上角数字。
  - 采购单管理：全部 `8`、待处理 `1`、会签中 `1`、已完成 `5`、已驳回 `1`，均显示右上角数字。
- 微信公众平台网页当前停留在扫码登录页，无法在未登录状态下核对版本列表；因此仅记录 CLI 上传成功，不把平台页面确认或体验版设置记作已验证。

## 现场复核补正（2026-09-09 01:13）

- 异常管理原先用“全时间范围”的待处理数作为页签角标，但列表默认查询本月，造成“角标有数字、列表无数据”。
- 已将异常角标查询统一带入一级、二级及报警起止日期；“待处理（当前筛）”改为使用同口径的全量计数，不再只统计当前分页。
- 线上以 2026-09-01 至 2026-09-09 复测：直售激活列表为空时页签数字同步为 0（按规则隐藏），Web 探活 HTTP 200。
- 现场截图进一步发现左侧“异常管理”仍为全时间/全异常维度统计，显示 15，而三个可见页签为 `0 + 4 + 5 = 9`。现已改为左侧仅汇总三个可见页签，并随页面代理及日期筛选结果同步更新。

## 全局角标复核（2026-09-09 01:20）

- 侧边栏只在存在待办意义的入口显示角标：二级审核、待分配、采购单管理、返货管理、异常管理；其余菜单不显示装饰性数字。
- 采购与返货原先来自 Dashboard 全历史、全代理统计，和默认本月列表不一致，且代理账号存在跨代理错计风险。现改为调用有数据权限的列表接口，并统一当前日期/代理筛选口径。
- 异常管理侧栏随三个可见页签合计实时更新；采购侧栏随“待处理+会签中”更新；返货侧栏随“一级退原厂待审核”更新。
- 顶部通知原先用最近 50 条列表计算未读数，超过 50 条会少计，且未按通知接收角色过滤。现增加独立未读总数接口，列表、单条已读、全部已读均限定当前角色受众。
- 线上管理员实测值：二级审核 `0`、待分配 `1`、采购待办 `0`、返货待办 `0`、异常待办 `9`、顶部未读 `0`。一级代理账号仅返回“一级”受众通知，不再看到原厂专属通知。

## 构建提示

- Web 仅有 Vite 大包体积提示，不影响构建。
- 小程序仅有 Sass 旧 API/`@import` 弃用提示，不影响构建。
