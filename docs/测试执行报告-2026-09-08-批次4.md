# 测试执行报告（批次 4：问题 #31–40）

执行日期：2026-09-08  
范围：管理端库存、返货、SN 联动及异常管理。

## 结果

| 问题 | 结果 | 验证证据 |
|---|---|---|
| #31 | 通过 | 库存汇总、SN 列表、库存流水页签均移除数量角标 |
| #32 | 通过 | 分销记录一级出库与二级入库；终端销售及退货记录对应库存流水 |
| #33 | 通过 | 仅“一级代理退原厂”存在待审核单时显示角标 |
| #34 | 通过 | 页签、列表和新增数据统一显示“终端退货” |
| #35 | 通过 | 操作统一为“通过/驳回”，两种操作均有二次确认 |
| #36 | 通过 | `done` 显示“已通过”，`rejected` 显示“已驳回”，筛选状态不再混用 |
| #37 | 通过 | 返货提交/通过/驳回写入 SN 生命周期；处理说明写回 SN，SN 情况说明在返货详情读取 |
| #38 | 通过 | 异常配置明确说明扫描会生成待处理异常且不修改库存 |
| #39 | 通过 | 移除超量比、周转倍数，配置弹窗改为内容高度 |
| #40 | 通过 | 销售库存异常可由二级代理归属补齐一级代理 |

## 自动验证

- Web UI 契约：13/13 通过（包含批次 4 两组）。
- Web `npm run build`：通过。
- 后端定向测试：13/13 通过。
- 后端全量测试：39/39 通过。

## 业务约束

- #32 分销是库存调拨，必须同时记录一级负向和二级正向流水；终端销售只记录实际持货层级的负向流水。
- #37 “情况说明”和“处理说明”继续分字段保存，不相互覆盖。
- #38 立即扫描只重新检查库存预警并创建异常，不修改任何库存数量。
- 本批未发布线上，最终全量回归后统一发布。
