# 测试执行报告（批次 2：问题 #11–20）

执行日期：2026-09-08  
范围：管理端 SN 码库与商品库。

## 结果

| 问题 | 结果 | 验证证据 |
|---|---|---|
| #11 | 通过 | 页面及弹窗移除系统生成 SN，只保留 Excel 段号导入 |
| #12 | 通过 | 出厂/销售/退货三组日期均使用不换行容器 |
| #13 | 通过 | 缩窄 SN、商品、尺寸，给状态和标签保留空间 |
| #14 | 通过 | 商品编码改为文本输入并直传既有 `productId` 查询 |
| #15 | 通过 | 去掉个性化/修理过，增加再入库；后端过滤旧业务标签 |
| #16 | 通过 | 对照原型，合法 SN 以 `RLyyyyMMdd` 为出厂日期；非法编码回退首次入码库时间 |
| #17 | 通过 | 历史已销售但无事件的 SN 在接口响应中补展示用销售生命周期，不在读取时篡改数据库 |
| #18 | 通过 | 情况/处理说明改为“日期 + 内容”并兼容旧字符串；所属二级标签不换行 |
| #19 | 通过 | 全选、尺码、输入框和添加按钮统一在对齐行内 |
| #20 | 通过 | 商品弹窗使用内容高度，不再保留固定空白 |

## 自动验证

- Web UI 契约：批次 2 共 4 组，4/4 通过。
- Web `npm run build`：通过。
- 后端 `SnTagsTest`、`SnFactoryDatesTest`、`SnServiceLifecycleTest`：通过。
- 后端 `mvn -DskipTests package`：通过。

## 数据说明

- #17 采用响应层兼容旧数据，不执行不可逆历史数据更新。
- #15 不删除数据库里的旧标签；筛选和展示统一隐藏，避免批量修改生产数据。
- 本批未发布线上，最终全量回归后统一发布。
