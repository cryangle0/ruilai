# 测试执行报告（批次 6：问题 #51–60）

执行日期：2026-09-08  
范围：管理端角色、账号、通知及小程序数据/扫码。

## 结果

| 问题 | 结果 | 验证证据 |
|---|---|---|
| #51 | 通过 | 仅账号数为 0 的角色显示删除；后端再次校验账号绑定 |
| #52 | 通过 | 平台账号可修改密码，新密码经 `PasswordEncoder` 保存 |
| #53 | 通过 | 通知点击后标记已读并进入对应业务列表 |
| #54 | 通过 | 日期筛选卡片化、当前在库移到图表前、图表可左右滑动 |
| #55 | 通过 | 首页内容增加底部安全间距，不再被底部菜单遮挡 |
| #56 | 通过 | 移除查询扫码，扫码模式改两列并展示商品名称和扫描进度 |
| #57 | 通过 | 出货和直销页均提供可操作的 SN 输入框及下一步客户流程 |
| #58 | 通过 | 直销可连续加入多个 SN，并统一校验、填写客户和激活 |
| #59 | 通过 | 手工录入使用页面受控输入及明确“输入加入”动作 |
| #60 | 通过 | 省市区使用微信联动地区选择器，路/号弄/室独立输入 |

## 自动验证

- Web UI 契约：16/16 通过。
- Web `npm run build`：通过。
- 小程序类型检查：通过。
- 小程序页面测试：13/13 通过；采购模型测试：5/5 通过。
- 微信小程序生产构建：通过。
- 后端定向测试：15/15 通过。

## 业务约束

- #51 前后端均禁止删除已绑定账号的角色，避免只依赖按钮隐藏。
- #58 多 SN 激活逐条调用既有事务接口；任一 SN 在提交前先统一预检。
- #60 三个省市区展示框共用微信原生联动地区选择，最终地址由六段拼接。
- 本批未发布线上，最终全量回归后统一发布。
